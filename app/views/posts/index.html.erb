<h1>Listing posts</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Date</th>
    <th>Patron request</th>
    <th>Consultation summary</th>
    <th>Resources used</th>
    <th>Status</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @posts.each do |post| %>
  <tr>
    <td><%= post.title %></td>
    <td><%= post.date %></td>
    <td><%= post.patron_request %></td>
    <td><%= post.consultation_summary %></td>
    <td><%= post.resources_used %></td>
    <td><%= post.status %></td>
    <td><%= link_to 'Show', post %></td>
    <td><%= link_to 'Edit', edit_post_path(post) %></td>
    <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Post', new_post_path %>
